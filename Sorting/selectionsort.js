{/* 

make a swap function
[1, 2, 30, 40]

*/}



const swap=(arr,x,y)=>{
    let temp=arr[x];
    arr[x]=arr[y];
    arr[y]=temp;
}

const selSort=(arr,size)=>{
    let i=0;
    while(i<n-1){
        let j=locOfSmallest(arr,i,n-1);
        i+=1;
    }
}

const locOfSmallest=(arr,i,N)=>{
    return 0;
}

let a=[1,2,30,40];

